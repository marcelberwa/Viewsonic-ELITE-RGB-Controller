# ViewSonic RGB Controller - Python Dependencies

# HID communication
pywinusb>=0.4.2

# HTTP requests for Razer Chroma SDK
requests>=2.28.0

# Screen capture for color sampling (optional, for chroma_screen_sync.py)
pillow>=10.0.0
numpy>=1.24.0

# Windows service support (optional)
pywin32>=305; sys_platform == 'win32'
